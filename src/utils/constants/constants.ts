export const MAX_FILE_SIZE_BYTES: number = 1024 * 1024 * 10;

export const FILE_FIELD_NAME = 'movies';

export const ALLOWED_MIME_TYPE = 'text/plain';

export const TEMP_LOCAL_FILE_PATH = './imports';

export const JWT_EXPIRES_IN = '1h';
